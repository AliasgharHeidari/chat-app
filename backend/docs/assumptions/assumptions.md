## technical assumptions
    1. no otp registration for now
    2. user account includes Name, last name, bio, ID, profile pic link, is online, action(is typing)
    3. only private chats are available for now
    4. login only requires ID, password for now
    5. message status(seen, sending, sent, falied) must be visible
    6. send, Remove, edit message must be possible
    7. if a message is edited there has to be a edited tag on the message.
    8. data will be stored on postgres DATABASE
    10. user ID must have at least 5 character.
    